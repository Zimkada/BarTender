# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env*.local
.env.production
.env.staging

# Supabase secrets
supabase/.env
supabase/functions/.env

# Scripts de configuration avec secrets (si régénérés)
setup-alerts.ps1
setup-alerts.sh
setup-alerts.bat
configure_postgres_settings.sql

.vercel

*storybook.log
storybook-static

# VSCode local history extension (contient parfois des copies de .env)
.history/

# Rapports d'audit / dead code (régénérables)
knip-report.txt
ts-prune-report.txt
git-log.txt
unpushed_commits_*.txt

# Fichiers temporaires Vite
vite.config.ts.timestamp-*.mjs
vite.config.ts.timestamp-*.js

# Marketing — stratégie commerciale confidentielle (repo public, ne pas exposer)
# Sauvegarder ces fichiers hors git (Drive, repo privé séparé).
marketing/
MARKETING.md
MARKETING.docx
Plan_Marketing_BarTender_Pro.doc

